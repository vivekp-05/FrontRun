{
  "name": "@frontrun/pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Workstream B \u2014 signal + enrichment. Detects real SEC Form D raises, enriches, drafts.",
  "type": "module",
  "scripts": {
    "detect": "tsx cli.ts",
    "typecheck": "tsc --noEmit -p .."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0"
  }
}
